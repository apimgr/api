{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2 tool-note">
      <a href="/">Home</a> / <a href="/testing">Testing Tools</a> / Postman Collection Generator
    </nav>
    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Postman Collection Generator</h1>
        <button class="btn btn-icon" data-favorite="testing-postman" title="Add to favorites">⭐</button>
      </div>
      <p class="tool-description">
        Describe an HTTP request and get back a minimal Postman Collection v2.1 JSON
        document containing that request.
      </p>
      <form id="postman-form" class="tool-form" data-body-endpoint="/api/v1/test/postman">
        <div class="form-group">
          <label class="form-label">Request (JSON)</label>
          <textarea name="body" class="form-input" rows="6" required placeholder='{"method":"GET","url":"https://example.com/api","headers":{"Accept":"application/json"}}'></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Generate</button>
      </form>
      <div id="postman-form-result" class="tool-result" hidden></div>
      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">POST Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl -X POST {{.BaseURL}}/api/v1/test/postman -d '{"method":"GET","url":"https://example.com/api"}'</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
