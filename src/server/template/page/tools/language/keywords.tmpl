{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2" style="color: var(--text-muted); font-size: 0.875rem;">
      <a href="/">Home</a> / <a href="/language">Language</a> / Keyword Extraction
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Keyword Extraction</h1>
        <button class="btn btn-icon" data-favorite="language-keywords" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Extract the most frequent non-stopword keywords from text.
      </p>

      <form id="keywords-form" class="tool-form" data-body-endpoint="/api/v1/language/keywords">
        <div class="form-group">
          <label class="form-label">Text</label>
          <textarea name="body" class="form-input" rows="8" required placeholder="Paste or type text here."></textarea>
        </div>

        <button type="submit" class="btn btn-primary">Extract Keywords</button>
      </form>

      <div id="keywords-form-result" class="tool-result" style="display:none;"></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">POST Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl -X POST {{.BaseURL}}/api/v1/language/keywords -d 'Some text to analyze for keywords.'</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
