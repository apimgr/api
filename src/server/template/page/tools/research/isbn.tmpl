{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2 tool-note">
      <a href="/">Home</a> / <a href="/research">Research</a> / ISBN Lookup
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">ISBN Lookup</h1>
        <button class="btn btn-icon" data-favorite="research-isbn" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Look up book metadata by ISBN using the free, keyless Open Library Books API.
      </p>

      <form id="isbn-form" class="tool-form" data-query-post-endpoint="/api/v1/research/isbn">
        <div class="form-group">
          <label class="form-label">ISBN</label>
          <input type="text" name="isbn" class="form-input" required placeholder="978-3-16-148410-0">
        </div>

        <button type="submit" class="btn btn-primary">Look Up</button>
      </form>

      <div id="isbn-form-result" class="tool-result" hidden></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">POST Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl -X POST "{{.BaseURL}}/api/v1/research/isbn?isbn=978-3-16-148410-0"</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
