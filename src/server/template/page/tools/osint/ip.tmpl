{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2 tool-note">
      <a href="/">Home</a> / <a href="/osint">OSINT Tools</a> / IP Intelligence
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">IP Intelligence</h1>
        <button class="btn btn-icon" data-favorite="osint-ip" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Look up geolocation and ISP information for a public IP address.
      </p>

      <form id="ip-form" class="tool-form" data-template="/api/v1/osint/ip/{ip}">
        <div class="form-group">
          <label class="form-label">IP address</label>
          <input type="text" name="ip" class="form-input" required placeholder="8.8.8.8">
        </div>

        <button type="submit" class="btn btn-primary">Investigate</button>
      </form>

      <div id="ip-form-result" class="tool-result" hidden></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">GET Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl {{.BaseURL}}/api/v1/osint/ip/8.8.8.8</pre>
          </div>
        </div>
      </div>

      {{template "geoip_attribution" .}}
    </div>
  </div>
</section>
{{end}}
{{end}}
