{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2" style="color: var(--text-muted); font-size: 0.875rem;">
      <a href="/">Home</a> / <a href="/validate">Validators</a> / Validate URL
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Validate URL</h1>
        <button class="btn btn-icon" data-favorite="validate-url" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Check whether a string is a correctly formatted URL.
      </p>

      <form id="validate-url-form" class="tool-form" data-query-post-endpoint="/api/v1/validate/url">
        <div class="form-group">
          <label class="form-label">Validate URL</label>
          <input type="text" name="url" class="form-input" required placeholder="https://example.com">
        </div>

        <button type="submit" class="btn btn-primary">Validate</button>
      </form>

      <div id="validate-url-form-result" class="tool-result" style="display:none;"></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">POST Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl -X POST "{{.BaseURL}}/api/v1/validate/url?url=https://example.com"</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
