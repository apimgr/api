{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2" style="color: var(--text-muted); font-size: 0.875rem;">
      <a href="/">Home</a> / <a href="/validate">Validators</a> / Validate IBAN
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Validate IBAN</h1>
        <button class="btn btn-icon" data-favorite="validate-iban" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Check an International Bank Account Number against the ISO 13616
        mod-97 checksum.
      </p>

      <form id="validate-iban-form" class="tool-form" data-query-post-endpoint="/api/v1/validate/iban">
        <div class="form-group">
          <label class="form-label">Validate IBAN</label>
          <input type="text" name="iban" class="form-input" required placeholder="GB29NWBK60161331926819">
        </div>

        <button type="submit" class="btn btn-primary">Validate</button>
      </form>

      <div id="validate-iban-form-result" class="tool-result" style="display:none;"></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">POST Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl -X POST "{{.BaseURL}}/api/v1/validate/iban?iban=GB29NWBK60161331926819"</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
