{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2" style="color: var(--text-muted); font-size: 0.875rem;">
      <a href="/">Home</a> / <a href="/convert">Unit Conversion</a> / Area Converter
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Area Converter</h1>
        <button class="btn btn-icon" data-favorite="convert-area" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Convert an area value between square meters/square feet and
        acres/hectares.
      </p>

      <form id="area-form" class="tool-form" data-template="/api/v1/convert/area/{value}/{from}/{to}">
        <div class="form-group">
          <label class="form-label">Value</label>
          <input type="number" name="value" class="form-input" step="any" required value="1">
        </div>

        <div class="form-group">
          <label class="form-label">From unit</label>
          <select name="from" class="form-input">
            <option value="sqm">Square meters (sqm)</option>
            <option value="sqft">Square feet (sqft)</option>
            <option value="acre">Acres</option>
            <option value="ha">Hectares (ha)</option>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label">To unit</label>
          <select name="to" class="form-input">
            <option value="sqft">Square feet (sqft)</option>
            <option value="sqm">Square meters (sqm)</option>
            <option value="ha">Hectares (ha)</option>
            <option value="acre">Acres</option>
          </select>
        </div>

        <button type="submit" class="btn btn-primary">Convert</button>
      </form>

      <div id="area-form-result" class="tool-result" style="display:none;"></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">GET Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl {{.BaseURL}}/api/v1/convert/area/1/sqm/sqft</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
