{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2 tool-note">
      <a href="/">Home</a> / <a href="/convert">Unit Conversion</a> / Pressure Converter
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Pressure Converter</h1>
        <button class="btn btn-icon" data-favorite="convert-pressure" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Convert a pressure value between pascals, bar, PSI, and
        atmospheres.
      </p>

      <form id="pressure-form" class="tool-form" data-template="/api/v1/convert/pressure/{value}/{from}/{to}">
        <div class="form-group">
          <label class="form-label">Value</label>
          <input type="number" name="value" class="form-input" step="any" required value="1">
        </div>

        <div class="form-group">
          <label class="form-label">From unit</label>
          <select name="from" class="form-input">
            <option value="pa">Pascals (pa)</option>
            <option value="bar">Bar</option>
            <option value="psi">PSI</option>
            <option value="atm">Atmospheres (atm)</option>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label">To unit</label>
          <select name="to" class="form-input">
            <option value="bar">Bar</option>
            <option value="pa">Pascals (pa)</option>
            <option value="psi">PSI</option>
            <option value="atm">Atmospheres (atm)</option>
          </select>
        </div>

        <button type="submit" class="btn btn-primary">Convert</button>
      </form>

      <div id="pressure-form-result" class="tool-result" hidden></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">GET Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl {{.BaseURL}}/api/v1/convert/pressure/1/pa/bar</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
