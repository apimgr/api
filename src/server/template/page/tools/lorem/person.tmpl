{{define "content"}}
{{$tool := .}}
{{with $tool}}
<section>
  <div class="container container-sm">
    <nav class="mb-2" style="color: var(--text-muted); font-size: 0.875rem;">
      <a href="/">Home</a> / <a href="/lorem">Lorem &amp; Fake Data</a> / Fake Person
    </nav>

    <div class="tool-card">
      <div class="tool-header">
        <h1 class="tool-title">Fake Person</h1>
        <button class="btn btn-icon" data-favorite="lorem-person" title="Add to favorites">⭐</button>
      </div>

      <p class="tool-description">
        Generate a fake person with a name, email address, and phone number
        for use in tests and mock data.
      </p>

      <form id="person-form" class="tool-form" data-template="/api/v1/lorem/person">
        <button type="submit" class="btn btn-primary">Generate</button>
      </form>

      <div id="person-form-result" class="tool-result" style="display:none;"></div>

      <div class="mt-3">
        <h3>API Endpoint</h3>
        <div class="code-block">
          <div class="code-header">
            <span class="code-lang">GET Request</span>
            <button class="btn btn-sm" data-copy title="Copy">Copy</button>
          </div>
          <div class="code-content">
            <pre>curl {{.BaseURL}}/api/v1/lorem/person</pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{{end}}
{{end}}
