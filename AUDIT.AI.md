# Project Audit

Started: 2026-07-20
Scope: line-by-line spec-compliance audit of `src/` against `AI.md` (PART 0-33 + FINAL), reconciled with `IDEA.md` non-goals.

Legend: `[x]` fixed this pass · `[ ]` open · `NEEDS DECISION` = product/architecture call required before fixing.

---

## Central architectural conflict (resolve FIRST — gates many items below)

`AI.md` (base spec) describes an admin panel, admin/config-password auth, user
sessions, and account emails. `IDEA.md` (project override, authoritative for
this project) lists these as explicit NON-GOALS:
- "No user accounts, registration, or login of any kind"
- "No admin web panel (server configured via server.yml only)"
- "No authentication on any endpoint"
- Excludes machine translation and language auto-detection

**RESOLVED:** IDEA.md confirmed authoritative (AI.md lines 11745, 15765,
17739, 23451; IDEA.md lines 39, 52, 60, 121 — both docs agree, not in
conflict). The following subsystems were REMOVED per this confirmation:
`src/admin/` (admin.go, pages.go, routes.go), `AdminConfig`/`RolesConfig` in
`src/config/config.go`, `src/session/` (session.go), `SendWelcomeEmail`/
`SendPasswordResetEmail` in `src/email/email.go`, and `DetectLanguage`/
`Translate` in `src/service/language/language.go`. All admin/auth line-items
below tied to this decision are now moot/removed.

---

## Pass 1: Security

- [x] backup: `src/backup/backup.go:184,225` used `pbkdf2.Key(..., 100000, sha256)` — PART 21 requires **Argon2id** KDF for AES-256-GCM backup key. Replaced with `argon2.IDKey` (t=1, m=64MiB, p=4, 32-byte key). Import `pbkdf2`+`sha256` → `argon2`. FIXED
- [x] admin: `src/admin/admin.go:16,208-238` bcrypt admin/config password — MOOT, `src/admin/` deleted entirely (IDEA.md lines 39,52 confirm no admin panel/auth). RESOLVED
- [x] admin: `src/admin/admin.go` plaintext password fallback + plaintext token storage — MOOT, `src/admin/` deleted. RESOLVED
- [x] database: `src/database/database.go` `api_keys` stores raw `key` column, not `key_hash` (SHA-256). Token retrievable from DB. HIGH — RESOLVED by removal, see Completed
- [x] session: `src/session/session.go` plaintext session token as PK — MOOT, `src/session/` deleted entirely (IDEA.md line 60: no sessions). RESOLVED
- [x] server: `src/server/ratelimit.go` + `src/ratelimit/ratelimit.go` trust `X-Forwarded-For` unconditionally (spoofable) — must only trust from configured proxy. HIGH — FIXED for `src/server/` (see Completed); `src/ratelimit/ratelimit.go` never actually inspected XFF/X-Real-IP (keys only on `RemoteAddr`+category) — original claim inaccurate for that file, see note in Completed
- [x] logger: `src/server/logger.go` logs `r.URL.RawQuery` unredacted — leaks secrets/tokens in query strings. HIGH — FIXED, see Completed
- [x] middleware: `src/server/middleware.go` CSP includes `unsafe-inline`/`unsafe-eval`; missing directives. Weakens XSS protection (PART 16/security). MEDIUM — FIXED, see Completed
- [x] metrics: `src/metrics/metrics.go` uses raw request path as label → unbounded cardinality → memory-exhaustion DoS. Must use route pattern. MEDIUM — FIXED, see Completed
- [x] session: `src/session/session.go:185` `Secure:false`/SameSite — MOOT, `src/session/` deleted. RESOLVED

## Pass 2: Code Quality

- [x] frontend: `src/server/template/layout/public.tmpl:2` `<!DOCTYPEhtml>` (missing space) — malformed doctype (`admin.tmpl` counterpart moot — file deleted, see admin removal). FIXED
- [x] frontend: `src/server/static/js/app.js:66` `if (!form)returnfalse;` — broken JS (would not parse/execute). FIXED
- [x] backup: `src/backup/backup.go:245` stale `// TODO: Return a streaming cipher reader` — `decryptedReader` is actually implemented; removed misleading TODO. FIXED
- [x] main: `src/main.go:177,178,610,630` TODO markers + stubbed update/service commands. HIGH — see Completed
- [x] session: `src/session/session.go:219` TODO marker; inline comment at :185 — MOOT, `src/session/` deleted entirely (IDEA.md line 60: no sessions). RESOLVED
- [x] service (text): `src/service/text/text.go` ~323-395 mass stubs — ID generators return fixed constants; Diff/Soundex/Metaphone/regex/compress/ciphers/extraction/line-ops/lorem all no-op. HIGH (see Completed section below)
- [x] service (crypto): `src/service/crypto/crypto_extended.go` **fake crypto** — GenerateRSAKeys/ECDSAKeys return literal header strings; AESEncrypt→"encrypted", RSAEncrypt/Decrypt fake, all nil error. CRITICAL (silent-wrong-output). FIXED
- [x] service: `src/service/{weather,osint,image,test}` all "not yet implemented" stubs. HIGH — FIXED, see Completed
- [x] database: `src/database/database.go` `RunMigrations()` TODO stub — spec says NO migrations table; delete it. MEDIUM — FIXED, see Completed
- [x] geoip: `src/geoip/geoip.go:134` TODO CIDR; index-out-of-range bug (guards `len>=3` then reads record[4-6]). HIGH — see Completed
- [x] css: `src/server/static/css/common.css:101` inline comment (must be above line). LOW — see Completed
- [x] sysservice: `src/sysservice/service.go` dead code (imported nowhere). MEDIUM — see Completed
- [x] service (network): `src/service/network/network.go` is a 0-byte empty file — entire subsystem missing (IDEA.md line 31: caller IP/header inspection, user-agent parsing, MAC vendor lookup, subnet calc, IPv6 ULA generation, random port suggestion). Repo-wide `go build ./...` fails on this package. CRITICAL (build-breaking) FIXED
- [x] service (test): `src/service/test/` has a package-name collision — `test.go` declares `package test`, `testing.go`/`testing_impl.go` declare `package testing`, all in the same directory. Does not compile. `testing.go` also has a bare `// TODO: Implement functions per SPEC.md section 3.X` (prohibited marker). Repo-wide `go build ./...` fails on this package. CRITICAL (build-breaking) FIXED

## Pass 3: Logic & Correctness

- [x] geoip: `src/geoip/geoip.go` index-out-of-range (see Pass 2) — runtime panic on short CSV rows. HIGH — see Completed
- [x] scheduler: `src/scheduler/scheduler.go` cron parsing fake — always uses 24h interval regardless of expression; no DB persistence/catch-up. HIGH — see Completed
- [x] config: `src/config/config.go` hardcoded Port "64365"; dead `generateRandomPort()`; spec wants random 64xxx first-run. MEDIUM — see Completed
- [x] config/bool: `src/config/bool.go` `ParseBool` signature diverges from spec `ParseBool(s string, defaultVal bool) (bool, error)`; incomplete truthy/falsy sets. MEDIUM — see Completed
- [x] ssl: `src/ssl/acme.go`+`ssl.go` renewal threshold 30 days (spec 7); no self-signed fallback; DNS-01 impossible via autocert. HIGH — see Completed

## Pass 4: Documentation Completeness

- [x] `.github/` ABSENT — PART 27 requires `ci.yml` + `release.yml`. HIGH — see Completed
- [x] README.md not verified in sync with CLI flags/config (many missing flags in main.go). MEDIUM — see Completed
- [x] CLAUDE.md "Current Project State" note is STALE (claims only PART 0-6 done; code covers far more). LOW — informational — see Completed

## Pass 5: Spec & Rules Compliance

- [x] Makefile: uses `golang:alpine` not `casjaysdev/go:latest`; missing `GOFLAGS=-buildvcs=false`; only builds server (missing `api-cli`). HIGH — see Completed
- [x] docker/Dockerfile: builder `golang:alpine` (should be casjaysdev/go); no non-root USER. MEDIUM (location correct) — see Completed
- [x] config: `src/config/config.go` invented DB driver default `"file"` (should be `sqlite`); missing `url` field + DATABASE_* env; missing `server.api_version`, `server.healthz.root.enabled`. HIGH — see Completed
- [x] main: `src/main.go` invented `API_*` env vars in help (spec: MODE/CONFIG_DIR/DATA_DIR/LOG_DIR/DEBUG/PID_FILE/PORT/LISTEN); missing flags (--shell,--cache,--backup,--baseurl,--color,--lang); env fallback chain not implemented. HIGH — see Completed
- [x] health: `src/server/handler/health.go` route `/server/healthz` + `/api/v1/server/healthz` MISSING (only /healthz, /api/v1/healthz, /api/v1/system/health). HealthResponse invents Node/Cluster/service and leaks `os.Hostname()`. Envelope format wrong (uses success/data/error citing "PART 36"; spec is `{ok:true,data}` / `{ok:false,error,message,details}`). HIGH — FIXED, see Completed
- [x] server: invented `/api/v1/system/*` endpoints (NO INVENTION violation). MEDIUM — FIXED, see Completed
- [x] email: `src/email/email.go` hardcoded `localhost:64580` dev URLs — the two offending functions (`SendWelcomeEmail`, `SendPasswordResetEmail`) deleted entirely, no callers existed (IDEA.md: no accounts/auth); remaining email funcs have no hardcoded URLs. Original finding RESOLVED. Also fixed a `go vet` IPv6-unsafe address finding (`fmt.Sprintf("%s:%d", ...)` → `net.JoinHostPort`) found during this pass. Still open (separate features, not this finding): no template system; no notifications config. MEDIUM
- [x] ratelimit: neither limiter matches spec limits (read=120/write=10/health=120/global_burst=240); wrong 429 body. MEDIUM — FIXED for `src/server/ratelimit.go` (exact spec limits + envelope), see Completed. NEEDS DECISION resolved: `src/ratelimit/ratelimit.go` (separate, redundant package) deleted entirely — its `Middleware()` was dead code (never wired to any handler) and its sole live caller, `CleanupOldEntries()` from `src/scheduler/tasks.go`, is unnecessary since the spec-compliant `src/server/ratelimit.go` has its own self-contained `classLimiter.cleanup()`; removed the call and the now-unused import from `tasks.go`
- [x] middleware: X-Frame DENY (spec SAMEORIGIN); HSTS 1yr (spec 2yr); missing COOP/COEP/CORP/NEL. MEDIUM — FIXED, see Completed
- [x] paths: `src/paths/paths.go` no GetCacheDir/--cache support; --backup writable fallback not implemented. MEDIUM (Docker path subdir + Linux non-root dirs VERIFIED CORRECT) — see Completed

## Pass 6: Code Flow / Missing subsystems

- [x] PART 32 CLIENT (`api-cli`): binary declared in IDEA.md (`client_binary: api-cli`). Makefile has conditional `api-cli` build targets (`build`/`local`/`dev`, guarded by `if [ -d "src/client" ]`) and `release.yml` has a matching `if: hashFiles('src/client/') != ''` step. FIXED: `src/client/` source now exists (`api/`, `cmd/`, `config/`, `output/`, `paths/`, `tui/`, `main.go`) and produces a real binary — build/run-verified in the `casjaysdev/go:latest` toolchain (`go build ./src/client`, exit 0; full-repo `go build`/`go vet ./...` also exit 0 per the final verification run). REQUIRED, no skip — satisfied.
- [x] geoip: wrong tech — CSV reader, not MMDB (`maxminddb-golang`) per spec; wrong data path. HIGH — see Completed
- [x] metrics: hand-rolled, not `client_golang`; wrong metric names; `_ms` not seconds. HIGH — FIXED, see Completed
- [x] logger: missing app.log (logfmt) / auth.log (syslog); no health-check log suppression; missing CEF format. MEDIUM — FIXED, see Completed

## Completed (out-of-spec removal pass)
- Deleted `src/admin/` (admin.go, pages.go, routes.go), `src/session/` (session.go), and the admin template tree (`template/layout/admin.tmpl`, `template/partial/admin/`, `template/page/admin/`) — no admin panel/sessions per IDEA.md lines 39,52,60 / AI.md lines 11745,15765,17739.
- Removed `admin.SetupRoutes(r, cfg)` call, its import, and the admin-page template-init block from `src/server/server.go`; removed dead `AdminEmail` PageData field/assignment (unused in templates).
- Removed `AdminConfig`/`RolesConfig` types, `Admin`/`Roles` fields, and their default-value blocks from `src/config/config.go`.
- Removed `session.CleanupExpired()` task, its `@hourly` registration, and the `src/session` import from `src/scheduler/tasks.go`.
- Removed `SendWelcomeEmail`/`SendPasswordResetEmail` from `src/email/email.go` (no callers; IDEA.md: no accounts).
- Removed `DetectLanguage`/`Translate` from `src/service/language/language.go` (no callers; IDEA.md line 121: translation/detection excluded). Kept `GetLanguageName`/`ListLanguages`.
- Removed `admin:` config block from `docs/configuration.md` (yaml example + env var example).
- Verified zero remaining references repo-wide via `grep -rn` for all removed symbols; targeted `go build` of touched packages (server, config, scheduler, email, language) passes clean in `casjaysdev/go:latest`.

## Completed (fixed this pass)
- service (network): `src/service/network/network.go` implemented from scratch (was 0-byte) per IDEA.md line 31 — `CallerInfo` (reflects RemoteAddr resolved by chi RealIP + surfaces caller-identifying headers, no blind XFF trust), `ParseUserAgent` (delegates to existing `src/service/parse` service), `MACVendor` (embedded offline IEEE OUI-prefix table, no network calls), `SubnetCalculate` (net/big.Int CIDR math: network/broadcast/first-last host/usable count, IPv4+IPv6), `GenerateULA` (RFC 4193 IPv6 unique-local /48 via crypto/rand), `RandomPort` (IANA dynamic/private range 49152-65535 via crypto/rand). Wired into HTTP at `/api/{version}/network/{ip,user-agent,mac/{mac},subnet,ula,port}` via new `src/server/api_network.go` using the PART 14 `{ok,data}`/`{ok,error,message}` envelope; deleted dead placeholder `src/service/network/network_impl.go`. Verified: `go build`/`go vet`/`gofmt` clean in `casjaysdev/go:latest`.
- service (test): `src/service/test/` package-collision resolved — `testing.go` (TODO stub) and `testing_impl.go` (dead placeholder) deleted; both declared `package testing` conflicting with `test.go`'s `package test` in the same directory. Fixed a pre-existing invalid-syntax method name in `test.go` (`GenerateMockAPI Response` → `GenerateMockAPIResponse`, matching its existing caller in `GenerateFixture`) and a gofmt alignment issue in `TestResult`. No functionality was missing — `test.go` already implemented the full testing-utility surface (mock data, assertions, benchmarking, fixtures); nothing needed merging from the deleted files. Verified: `go build`/`go vet`/`gofmt` clean in `casjaysdev/go:latest`.
- backup: PBKDF2→Argon2id in encrypt() and decrypt(); import + KDF-param constants; verified `go build ./src/backup/` clean, gofmt clean.
- backup: removed stale TODO comment (decryptedReader already implemented).
- frontend: `<!DOCTYPEhtml>`→`<!DOCTYPE html>` in public.tmpl and admin.tmpl.
- frontend: `if (!form)returnfalse;`→`if (!form) return false;` in app.js.
- Makefile: rewritten per PART 25 spec — `casjaysdev/go:latest` (was `golang:alpine`), `GOFLAGS=-buildvcs=false` + `CGO_ENABLED=0` set on the Docker build container, six required targets (`build local release docker test dev` + `clean` helper) matching spec exactly, `main.OfficialSite` ldflag added, conditional `api-cli` build steps guarded by `if [ -d "src/client" ]` (source doesn't exist yet — see Pass 6 PART 32 note). Verified: `make -n <target>` dry-run clean for all seven targets.
- docker/Dockerfile: builder image `golang:alpine`→`casjaysdev/go:latest` (+ `apk add git bash` per spec example); added non-root `app` user/group, `setcap cap_net_bind_service` on the `api` binary (PART 26 exception for privileged port 80: prefer setcap over root), `chown -R app:app /config /data`, and `USER app` before ENTRYPOINT. Scope limited to only these two named issues — LABEL blocks and other structure left untouched. Verified: `docker build --check` reports no warnings.
- `.github/workflows/ci.yml` created per PART 27 spec (lint/test/build/vuln-check jobs from the spec's literal example), extended with a mandatory `secret-scan` job (TruffleHog, pinned `trufflesecurity/trufflehog@27b0417c16317ca9a472a9a8092acce143b49c55` # v3.95.9) per the CI/CD Rules table ("Secret scanning is mandatory" on public repos) and a weekly `schedule: cron '0 6 * * 1'` trigger with `if: github.event_name != 'schedule'` guards on non-security jobs. All `actions/checkout` pinned to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` # v7.0.0 (SHA taken from AI.md, already used elsewhere in spec). Verified: `act --list -W .github/workflows/ci.yml` succeeds (5 jobs across 2 stages); `yq eval` syntax clean.
- `.github/workflows/release.yml` created verbatim per PART 27 spec (substituting `PROJECT_NAME: api`), 8-platform build matrix (linux/darwin/windows/freebsd × amd64/arm64), conditional `api-cli` build/upload steps, GitHub release creation. Actions pinned: `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` # v7.0.0, `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` # v7.0.1, `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` # v8.0.1, `softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b` # v3.0.1 (all SHAs from AI.md). Top-level `permissions: contents: read`, `contents: write` scoped to the `release` job only per the Workflow Permissions rules. Verified: `act --list -W .github/workflows/release.yml` succeeds (2 jobs across 2 stages); `yq eval` syntax clean.
- crypto: `src/service/crypto/crypto_extended.go` fake crypto replaced with real implementations — `GenerateRSAKeys` (crypto/rsa, PKCS#1/PKIX PEM), `GenerateECDSAKeys` (crypto/ecdsa P-256, PEM), `AESEncrypt`/`AESDecrypt` (AES-256-GCM with Argon2id key derivation, matching `src/backup/backup.go`'s KDF pattern), `RSAEncrypt`/`RSADecrypt` (RSA-OAEP/SHA-256); verified with round-trip tests (build, vet, gofmt clean in `casjaysdev/go:latest`).
- weather: `src/service/weather/weather.go` implemented against the free, keyless Open-Meteo API (geocoding-api.open-meteo.com + api.open-meteo.com, no API key) — `SearchLocation`, `GetCurrentWeather`, `GetWeatherByCoordinates`, `GetForecast` now make real HTTP calls (10s timeout) and map WMO weather codes to descriptions/icons; unit-conversion helpers were already correct and untouched. Signatures unchanged. Build/vet/gofmt clean.
- osint: `src/service/osint/osint.go` implemented per IDEA.md lines 34,87-90 — `WHOISLookup` (TCP/43 to whois.iana.org with referral follow-up), `DNSLookup` (system resolver, A/AAAA/MX/TXT/NS/CNAME), `IPLookup` (reuses the existing `src/geoip` local-DB lookup, no per-request outbound call), `SSLInfo` (direct TLS handshake, cert-only read, no data sent). Added `src/service/osint/ssrf.go`: `validateTarget`/`isBlockedIP` block loopback/link-local/private/unspecified/multicast targets (and `localhost`) before any WHOIS or TLS connection; `DNSLookup`/`IPLookup` reject literal private/loopback IP input directly. Signatures unchanged. Build/vet/gofmt clean.
- image: `src/service/image/image.go` + new `src/service/image/image_transform.go` implemented with pure Go stdlib only (`image`, `image/draw`, `image/png`, `image/jpeg`, `image/gif` — no CGO, no new dependency). **Signature change (NEEDS DECISION confirmed as spec-incompatible, not guessed):** original `Resize(width, height int) error` / `Crop(x, y, width, height int) error` / `Rotate(degrees int) error` had no way to receive image data in or return results out — functionally incapable of doing real work. `Service` was made stateful (`img image.Image` field) with new `Load([]byte) error`, `LoadFile(path string) error`, and `Bytes(format string) ([]byte, error)` methods; `Resize`/`Crop`/`Rotate` now operate on `s.img` (nearest-neighbor resize, bounds-checked crop, arbitrary-angle nearest-neighbor rotate with auto-expanding canvas). `GetInfo(imagePath string)` implemented via `image.DecodeConfig` + `os.Stat` (signature unchanged). Added `GeneratePlaceholder(width, height int, format, bgColorHex string) ([]byte, error)` to cover IDEA.md line 36's "placeholder image generation" (not present in the original stub at all). No existing callers reference this package (grep confirmed), so the signature change has zero blast radius. Build/vet/gofmt clean.
- css: `src/server/static/css/common.css:101` moved `/* Pushes footer to bottom per PART 17 */` from an inline trailing comment on `flex: 1;` to its own line directly above the declaration, per project comment convention (comments always above, never inline).
- sysservice: `src/sysservice/service.go` was complete, spec-correct (per AI.md PART 4 Service row: systemd/runit/launchd/Windows Service Manager/BSD rc.d) but never imported — main.go's `--service` handlers were a hand-rolled, Linux-only/systemd-only duplicate. Wired in: added missing `Disable()` function to `sysservice`; rewrote `installService`/`uninstallService`/`startService`/`stopService`/`restartService`/`reloadService`/`disableService` in `src/main.go` to delegate to `sysservice.Install/Uninstall/Start/Stop/Restart/Reload/Disable`; added `sysservice` import, removed now-unused `os/exec` import. Verified: `go build`/`go vet` clean for `src/paths/...` and `src/sysservice/...` in isolation; full-repo build errors present are confined to unrelated in-flight packages (`service/math`, `server`, `scheduler`) being edited by other concurrent agents, not this change.
- paths: `src/paths/paths.go` added `GetCacheDir()`/`InitCache()`/`CacheDir()` per AI.md PART 4's Cache row (Docker: `/data/{project}/cache`; Linux/BSD privileged: `/var/cache/{org}/{project}`; macOS privileged: `/Library/Caches/...`; Windows privileged: `{ProgramData}/{org}/{project}/cache`; Linux/BSD user: `${XDG_CACHE_HOME:-~/.cache}/{org}/{project}`; macOS user: `~/Library/Caches/...`; Windows user: `{LocalAppData}/{org}/{project}/cache`). Implemented the `--backup` writable fallback: `GetBackupDir()` now prefers the system backup path if `isWritable()`, else falls back to `{DataDir}/backup` when the process started elevated, else the user backup dir — never flips privilege mode mid-run (`startedElevated` captured once at package init). Extracted `systemBackupDir()`/`userBackupDir()` helpers preserving original per-OS paths; added `isWritable()` (MkdirAll + temp-file probe). `--cache`/`--backup` flags and `paths.InitCache`/`InitBackup` wiring in `main.go` were added by a concurrently-running agent (not duplicated here). Verified: `go build`/`go vet ./src/paths/...` clean in `casjaysdev/go:latest`.
- README.md: fixed drift from actual `src/config/config.go` and `src/main.go` — the `rate_limit:` example used a flat `requests`/`window` pair that no longer exists (real `RateLimitConfig` has `read`/`write`/`health` sub-blocks + `global_burst`, corrected to match defaults); removed a fictional top-level `api:` config section (`services`/`limits`) that has no backing struct anywhere in `config.go` (`Config` only has `server`/`web`); added CLI examples for the `--config`/`--data`/`--log`/`--cache`/`--backup` directory-override flags (present in main.go, previously undocumented); updated the Features bullet from "Systemd service integration" to "Cross-platform service integration (systemd, runit, launchd, Windows Service Manager, BSD rc.d)" to reflect the sysservice wiring above. Accuracy fixes only, not a rewrite.
- CLAUDE.md: "Current Project State" section rewritten from the stale "Bootstrap pass (PART 0-6) completed" note to reflect actual repo state — checked via `git status --porcelain` (dozens of modified/deleted/new files spanning admin/session removal, paths, sysservice, CI/CD, Makefile, Dockerfile, multiple service packages) and `AUDIT.AI.md`'s Completed sections; now references `AUDIT.AI.md` as the authoritative itemized status instead of re-duplicating a point-in-time list.
- service (text): `src/service/text/text.go` ~323-395 mass stubs replaced with real implementations, all stdlib-only (no new go.mod deps) — ID generators (`ULID` Crockford base32, `NanoID` 21-char crypto/rand, `KSUID` base62 via math/big, `XID` base32hex machine+pid+counter, `CUID` base36 blocks, `Snowflake` bit-packed timestamp+machine+seq, `ObjectID` hex 12-byte); `Diff` (LCS-based unified-diff line output); `Levenshtein`/`Similarity` (DP edit distance); `Soundex` (standard 4-char code, H/W-transparent coalescing, verified `Robert`/`Rupert`→`R163`, `Ashcraft`→`A261`); `Metaphone` (simplified Lawrence-Philips rules, verified `Smith`→`SM0`, `Knight`→`NKT`, `Phone`→`FN`); `Compress`/`Decompress` (gzip/zlib/flate via compress/*, base64-wrapped); `RegexMatch`/`RegexReplace`/`RegexExplain` (regexp package); `MarkdownToHTML`/`MarkdownTOC`/`BBCodeToHTML`/`HTMLToText` (regex-based); `ROT47`/`Caesar`/`Vigenere`/`Binary`/`Morse` (classic non-cryptographic ciphers, distinct from `src/service/crypto/`); `ExtractEmails`/`ExtractURLs`/`ExtractIPs`/`ExtractPhones` (regexp + net.ParseIP validation); `Lines`/`Dedupe`/`Sort`/`Shuffle` (real line ops, non-mutating); `StripHTML`/`StripMarkdown`/`StripANSI`; `Slugify`/`ToPascalCase`; 7 themed Lorem generators (`Hipsum`/`Bacon`/`Cupcake`/`Pirate`/`Zombie`/`Corporate`/`TechIpsum`) with word/sentence/paragraph modes, kept independent from the pre-existing generic `src/service/lorem` faker service. All original function signatures preserved (only 3 existing call sites in `src/server/handler/text.go` — ULID/NanoID/KSUID — confirmed matching). No TODO/stub markers remain. New `src/service/text/text_test.go` added: 22 table-driven test functions, all passing. Verified `go build`/`go vet`/`gofmt -l` clean for `src/service/text/...` in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
- geoip: `src/geoip/geoip.go` rewritten from a CSV reader to MaxMind's MMDB format via `github.com/oschwald/maxminddb-golang` (added to go.mod/go.sum), fixing the wrong-technology finding and the index-out-of-range panic on short rows. Added graceful degradation per IDEA.md line 96 — if the MMDB is unavailable/unloaded, geo/network lookups return IP-only results (no error) instead of panicking or failing other tools. Verified: `go build`/`go vet` clean for `src/geoip/...` in `casjaysdev/go:latest`.
- scheduler: `src/scheduler/scheduler.go` + new `src/scheduler/cron.go` — replaced the fake fixed-24h interval with a real hand-rolled 5-field cron parser (`* */N 1-5 JAN-DEC MON-FRI`, `@hourly`/`@daily`/`@weekly`/`@monthly`, `@every X` — no external cron library, no CGO, per AI.md PART 18 and project NEVER-do rule #5), including POSIX day-of-month/day-of-week OR-matching semantics. Added DB persistence (`src/database/scheduler.go`: `scheduler_tasks`/`scheduler_history` tables, `UpsertSchedulerTask`/`GetSchedulerTask`/`RecordSchedulerRun`) so enabled/next_run/last_run survive restarts, plus startup catch-up (`runCatchUp`, 1-hour catch-up window, tasks missed beyond the window are skipped to their next occurrence instead of run) and a 3-attempt exponential-backoff retry policy (5m/10m/20m) on task failure. Verified: `go build`/`go vet` clean for `src/scheduler/...` and `src/database/...` in `casjaysdev/go:latest`.
- ssl/acme: fixed all three named findings. (1) Renewal threshold corrected from 30 to 7 days (`ShouldRenew`, `src/ssl/acme.go`) per AI.md PART 15 (lines 20278, 20287, 27019); `CheckCertificateExpiry`/`GetCertificateInfo` rewritten to actually load and parse the certificate (`crypto/x509`) instead of returning a hardcoded 90-day placeholder. (2) Self-signed fallback added: new `src/ssl/selfsigned.go` (`GenerateSelfSigned`, stdlib-only ECDSA P-256 cert, no new dependency) is now used by `Manager.GetTLSConfig` as the last resort when no cert is found and Let's Encrypt is disabled, and by `getLetsEncryptTLSConfig`'s wrapped `GetCertificate` when autocert's live ACME issuance fails at handshake time — satisfying IDEA.md/PART 15's Tor/I2P self-signed requirement and the "no fallback when ACME unavailable" gap. Also filled in the previously-missing tier-3 (`{config_dir}/ssl/letsencrypt/{fqdn}/`) and exact tier-4 (`{config_dir}/ssl/local/{fqdn}/{cert.pem,key.pem}`) paths in `findExistingCerts`/`findManualCerts`, completing the spec's 4-tier priority chain. (3) DNS-01 is honestly gated rather than silently mishandled: `getLetsEncryptTLSConfig` detects a `dns-01` challenge config and falls back to self-signed with a log message, since `autocert` (the ACME client actually wired up) only supports HTTP-01/TLS-ALPN-01. `PerformDNS01Challenge`/`PerformHTTP01Challenge`/`PerformTLSALPN01Challenge` in `acme.go` now return explicit "not implemented" errors instead of logging-and-returning-nil (false-success bug). ADDENDUM (later pass): the dead `ACMEClient` struct plus `NewACMEClient`/`ObtainCertificate`/`(ac *ACMEClient) RenewCertificate` (confirmed via repo-wide grep — zero callers outside their own definitions, bodies were TODO-numbered stubs) were deleted outright from `acme.go`, along with their now-unused `crypto/tls` and `golang.org/x/crypto/acme` imports; the file's real, externally-called functions (`RenewalTask`, `CheckCertificateExpiry`, `ShouldRenew`, `GetCertificateInfo`, `loadLeafCertificate`, `AutoRenewInterval`, the three `Perform*Challenge` functions above) were left untouched. Re-verified clean: `gofmt -l` silent, `go build`/`go vet`/`go test ./...` all exit 0. **NEEDS DECISION:** full DNS-01 multi-provider support (AI.md PART 15 "ALL DNS providers", `go-acme/lego` integration, encrypted per-provider credential storage in `server.tls.dns_credentials.*`, dynamic per-provider field validation) is a substantial new architecture surface — new dependency, config schema, and credential-encryption design — not implemented; requires a product decision on provider scope before building. Separately noted but explicitly out of this pass's scope: `ssl.Manager`/`autocert` are not wired into `main.go` at all (no `ListenAndServeTLS`/`:443` call found anywhere), and `src/scheduler/tasks.go`'s `sslRenewalTask()` still uses a hardcoded flat `{data_dir}/ssl/cert.pem` path inconsistent with the new tiered layout — both are main.go/wiring-level gaps for a separate pass, not `src/ssl/` package-level bugs. Verified: `gofmt -l` clean; `go build`/`go vet` for `src/ssl/...` run in `casjaysdev/go:latest` (`-e CGO_ENABLED=0 -e GOFLAGS=-buildvcs=false`).
- config/bool: `src/config/bool.go` `ParseBool` rewritten to match the spec signature `ParseBool(s string, defaultVal bool) (bool, error)` with the full truthy (`1,t,true,y,yes,on`) / falsy (`0,f,false,n,no,off`) set, case-insensitive; empty string returns `defaultVal, nil`, unrecognized values return an error. Verified: `go build`/`go vet`/`gofmt` clean for `src/config/...`.
- config: `src/config/config.go` fixed the DB-driver/random-port/missing-key findings — DB driver default corrected `"file"` → `"sqlite"`; added `Database.URL` field (default `{data_dir}/db/server.db`) plus `applyDatabaseEnvOverrides` honoring `DATABASE_DRIVER`/`DATABASE_URL`/`DATABASE_DIR` on every `Load()`; added `server.api_version` (default `v1`) and `server.healthz.root.enabled` (default `false`, new `HealthzConfig`/`HealthzRootConfig` types); replaced the hardcoded `Port: "64365"` with the previously-dead `generateRandomPort()` (random 64000-64999), now actually called from `defaultConfig()` so a fresh install gets a random port once, persisted to `server.yml` thereafter via the existing first-run `Save()` path. Also added `server.update` (`UpdateConfig{Branch, AutoInstall, DeferDays}`, default `branch: stable`) per PART 22, backing the main.go `--update branch` persistence below. Also fixed a pre-existing (not self-introduced) gofmt violation: `type SecurityLogConfig struct{` → `struct {`. Verified: `gofmt -l` clean; `go build`/`go vet` clean for `src/config/...` in `casjaysdev/go:latest`. **Flagged, not fixed (out of scope):** `src/database/database.go`'s `Init()` still hardcodes its own SQLite path/driver instead of reading `cfg.Server.Database.Driver/URL` — a pre-existing wiring gap between the config and database packages, not part of this finding.
- main: `src/main.go` TODO markers and invented env vars resolved. (1) `--daemon`/`--pid` stubs replaced with real logic: new `src/daemon_unix.go`/`src/daemon_windows.go` (`daemonize()` — Unix forks/re-execs with `Setsid`+`_DAEMON_CHILD=1` marker per AI.md PART 8 "Daemonization"; Windows is a documented no-op warning, per spec) and a new `src/pidfile` package (`WritePIDFile`/`RemovePIDFile`/`CheckPIDFile` + `//go:build` split `process_unix.go`/`process_windows.go` for `isProcessRunning`/`isOurProcess`, container-aware via `paths.IsRunningInContainer`) plus `paths.DefaultPIDPath()` for the OS-specific default. (2) `--update branch {name}` now persists `server.update.branch` via `config.Load()`/`config.Save()` instead of a TODO no-op; `--maintenance mode {mode}` now persists `cfg.Server.Mode` the same way. (3) `printHelp()`'s invented `API_MODE`/`API_CONFIG`/`API_DATA`/`API_LOG`/`API_DEBUG` env var block replaced with the canonical list (`MODE`, `CONFIG_DIR`, `DATA_DIR`, `CACHE_DIR`, `LOG_DIR`, `BACKUP_DIR`, `PID_FILE`, `PORT`, `LISTEN`, `DEBUG`) matching AI.md PART 8's `--help` template; added the missing `--shell`, `--cache`, `--backup`, `--baseurl`, `--color`, `--lang` flags to both the flag parser and the Server Options help text. (4) Env-var fallback chain (CLI flag > env var > default) implemented consistently for all directory/network flags via a new `envOrFlag()` helper (`src/clihelpers.go`); `--color`/NO_COLOR resolved via `applyColorMode()` (CLI flag > `NO_COLOR` env > `TERM=dumb` auto-detect); `--lang` resolved via new `appmode.SetLang`/`GetLang`; `--shell completions|init|help [SHELL]` implemented in new `src/shellcmd.go`, mirroring the existing client-side completion pattern (`src/client/cmd/completions.go`) for the server binary's own flag set. **Scope boundary decisions (not silent, flagged for follow-up):** `colorEnabled` is resolved but not yet threaded through every `fmt.Printf`'s hardcoded emoji output in main.go — full NO_COLOR/emoji gating across the whole file was judged out of scope for "add the flag with working fallback chain." No config-file `output.color` override was wired (no such config type exists in this repo). A pre-existing dangling `// Generate secure password` comment with no function body at the end of main.go (introduced by a different concurrent agent's in-progress edit, not this pass) was left as-is — flagged, not fixed, since it isn't part of these three findings and other agents are actively editing this same file. Verified: `gofmt -l` clean on all touched/created files (`main.go`, `mode.go`, `paths.go`, `pidfile/*.go`, `daemon_unix.go`, `daemon_windows.go`, `shellcmd.go`, `clihelpers.go`, `config.go`); `go build`/`go vet` clean for `src/mode/...`, `src/paths/...`, `src/pidfile/...`, `src/config/...` in isolation in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false -e CGO_ENABLED=0`). Full-repo `go build ./...` from `src/main.go`'s import chain currently fails only on a pre-existing, unrelated issue in `src/metrics` (`prometheus/client_golang` listed `// indirect` in go.mod but imported directly) — owned by a different concurrently-running background agent's task area, not introduced by or fixable within this pass's scope.
- database: `src/database/database.go` — removed the `api_keys` table (raw, unhashed `key` column) entirely, along with its two indexes (`idx_api_keys_key`, `idx_api_keys_expires`), per IDEA.md's explicit "No API keys" non-goal and zero read/validate call sites anywhere in the codebase (same architectural precedent as the already-removed `src/admin/`/`src/session/`). Also removed `RunMigrations()` (a TODO stub) entirely — AI.md PART 10 mandates no migrations table/version tracking, idempotent `CREATE TABLE IF NOT EXISTS`/`ALTER TABLE` on every startup instead. Removed its sole caller in `src/main.go` (the `database.RunMigrations()` block after `database.Init()`). Removed the matching `DELETE FROM api_keys` cleanup block from `src/database/cleanup.go`'s `CleanupExpiredTokens()` (password-reset/email-verification cleanup untouched). Updated the stale `users.db: User data (admins, users, API keys)` doc comment. Verified repo-wide via `grep -rn "api_keys\|RunMigrations"` — zero remaining references. Verified: `go build`/`go vet ./src/database/...` clean, `gofmt -l` clean on all three touched files, in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
- server: `src/server/ratelimit.go` completely rewritten from a single flat sliding-window limiter to per-class (`read`/`write`/`health`) sliding-window limiters plus an absolute `global_burst` ceiling, matching AI.md's exact limits (read 120/60s, write 10/60s, health 120/60s, global_burst 240/60s) via the config's nested `RateLimitConfig` (`Read`/`Write`/`Health`/`GlobalBurst`). 429 response body corrected to the standard envelope `{"ok":false,"error":"RATE_LIMITED","message":"Too many requests"}`, wait time conveyed only via `Retry-After` header (no `retry_after` body field). Root-caused the XFF-spoofing finding to chi's stock `middleware.RealIP` (used unconditionally in `server.go`'s middleware chain) rather than to the rate limiter itself: replaced with a new trusted-proxy-gated `realIPMiddleware` (`src/server/realip.go`) that only trusts `X-Forwarded-For`/`X-Real-IP` when the immediate TCP peer is in a configured trusted-proxy allow-list (always-trusted private/loopback/link-local ranges + configurable `server.trusted_proxies.additional`); `getClientIP` in ratelimit.go now just reads the already-trust-evaluated `RemoteAddr`. `src/ratelimit/ratelimit.go` (separate, unrelated package) was confirmed to never actually inspect XFF/X-Real-IP at all (keys purely on `r.RemoteAddr + ":" + category`), so the original finding's XFF claim did not apply to that file; its `Middleware()` was dead code (never wired to any handler), and its category model/429 body were non-compliant with spec. ADDENDUM (later pass): rather than reconcile it, the package was deleted outright — the only live call site anywhere, `ratelimit.CleanupOldEntries()` in `src/scheduler/tasks.go`'s log-rotation task, was removed along with the now-unused import, since `src/server/ratelimit.go`'s spec-compliant limiter already does its own self-contained `classLimiter.cleanup()`. NEEDS DECISION resolved. Verified: `go build`/`go vet ./src/server/... ./src/scheduler/...` clean, `gofmt -l` clean, in `casjaysdev/go:latest`.
- logger: `src/server/logger.go` — implemented AI.md PART 11's Output Sanitization Pipeline query-param redaction (`token`/`session`/`code`/`key`/`password`/`secret`/`auth`/`pwd`/`api_key`/`apikey`/`access_token`/`refresh_token`, case-insensitive) via new `redactQuery()`, applied at both leak points (`LogAccess`'s JSON branch and `formatCustom`'s `{query}` substitution). Added `app.log` (logfmt, e.g. `time=... level=INFO msg="..." key=value`) and `auth.log` (RFC 3164 syslog, e.g. `Jan _2 15:04:05 hostname api[pid]: auth: ...`) log files and their `LogApp`/`LogAuth` writer methods, matching the spec's exact example formats. Added health-check log suppression (`isHealthCheckPath` against the actual registered routes in `server.go`: `/healthz`, `/server/healthz`, `/api/healthz`, `/api/v1/server/healthz`, `/api/v1/healthz`, `/api/v1/system/health`) — gated on `mode.IsDebugEnabled()` and 2xx-only, never suppressed on non-2xx or when debug is enabled. Added the missing `cef` case to `LogSecurity()`'s format switch (Common Event Format, `CEF:0|apimgr|api|1.0|...`). Wired the previously dead-code `loggingMiddleware` (`src/server/logging_middleware.go`) into `server.go`'s router chain (`r.Use(middleware.Logger)` → `r.Use(loggingMiddleware)`), so the whole file-based logging pipeline — including these fixes — now actually runs on every request; `middleware.Recoverer`/`middleware.Compress` keep the chi `middleware` import in use. Verified: `go build`/`go vet ./src/server/...` clean, in `casjaysdev/go:latest`.
- health: `src/server/handler/health.go` rewritten to PART 13's canonical `HealthResponse` — removed invented `Node`/`Cluster`/`service` fields and the `os.Hostname()` leak (Tier 1 violation per PART 11's Public Endpoint Safety Principle: internal hostnames are never public); response is now the flat JSON structure the spec defines (not wrapped in the generic `{ok,data}` API envelope — `/server/healthz` is a dedicated monitoring endpoint, not a PART 14 API route). `src/server/server.go` now registers the three required routes: unversioned `/api/healthz`, canonical `/server/healthz`, and versioned `/api/{api_version}/server/healthz`; the plain `/healthz` alias was kept (common convention, harmless, not spec-prohibited). Deleted the invented `/api/v1/system/*` route group (`system.health`/`system.info`/etc. — a NO INVENTION violation, no such surface exists in PART 14's API structure); nothing from it needed folding into healthz — the canonical `HealthResponse` already covers version/uptime/mode/db/metrics per PART 11 Tier 2. `src/service/system/health.go` (the handler's former backing service) is now unreferenced anywhere in the tree (`grep -rln "service/system"` returns nothing) — **flagged, not deleted**: out of this pass's 4-item scope, left as dead code for a follow-up cleanup pass. Verified: `go build -o /tmp/api-bin ./src/` and `go vet ./src/metrics/... ./src/server/...` both clean in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
- metrics: `src/metrics/metrics.go` fully rewritten from a hand-rolled counter/gauge implementation to `github.com/prometheus/client_golang/prometheus` + `promhttp`, exposing PART 20's required metric names/categories with correct `_seconds` (not `_ms`) units on duration histograms. `src/server/logging_middleware.go` fixed to label HTTP-request metrics with the matched chi route pattern (`chi.RouteContext(r.Context()).RoutePattern()`) instead of the raw request path, closing the unbounded-cardinality memory-exhaustion vector (an attacker hitting `/foo/<random>` no longer creates a new label series per request). `go.mod`/`go.sum` updated for the new dependency. Verified: `go build -o /tmp/api-bin ./src/` and `go vet ./src/metrics/... ./src/server/...` both clean in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
- middleware: `src/server/middleware.go`'s `securityHeadersMiddleware` rewritten per PART 11 "Security Headers" (AI.md line 13894). CSP: dropped `unsafe-inline`/`unsafe-eval` from `script-src` (kept `unsafe-inline` only on `style-src`, matching the file's actual inline-style usage), expanded to the full directive set (`img-src`/`font-src`/`connect-src`/`media-src`/`worker-src`/`manifest-src`/`frame-src`/`frame-ancestors`/`base-uri`/`form-action`/`object-src`, `upgrade-insecure-requests` gated on `cfg.Server.SSL.Enabled`, `report-to`/`report-uri`). `X-Frame-Options` changed `DENY`→`SAMEORIGIN` (spec keeps both `X-Frame-Options` and CSP `frame-ancestors 'self'` set together for legacy-browser compatibility). HSTS `max-age` `31536000`→`63072000` (2yr, RFC 6797 preload-list eligible), only set when SSL is enabled. Added the previously-missing `X-Permitted-Cross-Domain-Policies`, `Origin-Agent-Cluster`, `Cross-Origin-Opener-Policy`/`Cross-Origin-Embedder-Policy`/`Cross-Origin-Resource-Policy` (all at the spec's "everyone" default, e.g. COOP `unsafe-none` — line 14069 — tightening to `same-origin` is a compliance-flag feature, PART 16, not implemented), and `Reporting-Endpoints`/`Report-To`/`NEL`, all pointing at a request-aware URL (`scheme://r.Host/api/v1/server/reports/{default,csp}`, built inline via `r.TLS`/`X-Forwarded-Proto` scheme detection matching the existing `{fqdn}` pattern in `logger.go`) rather than a hardcoded bare path, per project-rules.md's NEVER-hardcode-bare-paths rule. Expanded `Permissions-Policy` to the PART 11 per-feature default table. Removed all stale TODO comments. **Flagged, not implemented (out of scope):** the `/api/{api_version}/server/reports/{default,csp}` endpoints these headers now reference do not exist yet (separate PART 11 Reporting API feature, no receiving handler anywhere in the tree); headers are still emitted per the spec's unconditional "MUST include" language — browsers that fail to reach the endpoint simply drop the report, no functional regression. Config-driven per-project header tightening (`web.headers`/`web.csp`/`web.permissions_policy`) also does not exist in `config.go` yet — current implementation is the static "everyone" default set, not yet wired to `server.yml`. Verified: `go build -o /tmp/api-bin ./src/` and `go vet ./src/metrics/... ./src/server/...` both clean in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
- logger: `src/server/logger.go`'s `healthCheckPaths` map (used by `isHealthCheckPath` for access-log suppression) was desynced from the routes actually registered in `server.go` after the invented-route removal above — follow-on correctness fix, not one of the original findings. Removed the two now-nonexistent entries (`/api/v1/healthz`, `/api/v1/system/health`), keeping the map in exact sync with the four routes `server.go` registers (`/healthz`, `/server/healthz`, `/api/healthz`, `/api/v1/server/healthz`). Verified: `go build -o /tmp/api-bin ./src/` and `go vet ./src/metrics/... ./src/server/...` both clean in `casjaysdev/go:latest` (`-e GOFLAGS=-buildvcs=false`).
