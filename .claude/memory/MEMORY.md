# Project Memory Index

Durable, project-specific knowledge for `api` that exists only because of this
codebase's own history — decisions made, gotchas discovered, and conventions
unique to this project.

Not a duplicate of `AI.md` (the spec) or `.claude/rules/` (spec cheatsheets).
Add an entry only after a nontrivial decision, workaround, or discovered fact
that would otherwise need re-explaining in a future session.

Each entry is its own markdown file in this directory with YAML frontmatter
(`name`, `description`, `type: project`), one topic per file, listed below.

Never store secrets, tokens, or passwords here — the credential masking rule
(`key=xxxxx`) applies exactly as it does everywhere else in the repo.

## Entries

None yet.
